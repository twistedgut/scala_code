# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET         /           controllers.Application.index
POST        /box        controllers.BoxesEndpoint.create

#PUT /quantity/inc/:box-code/:quantity    controllers.QuantityEndpoint.update(boxCode: String, quantity: Int)
PUT /quantity/inc/:box-code    controllers.QuantityEndpoint.update(boxCode: String)
